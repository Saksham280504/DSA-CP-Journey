#include <bits/stdc++.h>
using namespace std;

// #define int long long  => when use this convert int main()  to int32_t main() 
// #define endl '\n'

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    // These 3 lines are provided for faster I/O during tests. They basically bring optimization.

#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);    // Reads from input.txt
    freopen("output.txt", "w", stdout);  // Writes to output.txt
#endif

    // Sample Code
    int a, b;
    cin >> a >> b;
    cout << a + b << endl;

    return 0;
}
