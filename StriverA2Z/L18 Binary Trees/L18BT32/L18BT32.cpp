#include <bits/stdc++.h>
using namespace std;
// #define int long long  => when use this convert int main()  to int32_t main()
// #define endl '/n'

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif

    // your code here

    // Requirements to construct a unique binary tree:
    // 1) Inorder traversal (Having an information of what is present on the right and left of the root helps the most in constructing the unique binary tree)
    // 2) Pre/Postorder traversal

    return 0;
}
